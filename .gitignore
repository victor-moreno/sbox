# per-project dirs created by the sandbox itself
.claude/
.cache/
.tmp/

# keep paths specific to linux/mac
paths.conf

# local symlink, not part of the repo
TTS
.hermes
test/

# local planning files (plan-with-files), not pushed
task_plan.md
findings.md
progress.md

# "Always" answers from the network filter dialog
net-allow.conf

# planning files: local only
task_plan.md
findings.md
progress.md
# runtime network decisions (see lib/netproxy.py)
net-allow.conf

# python bytecode caches
__pycache__/
*.pyc
